import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Baku Legend Travel - Your Gateway to Azerbaijan',
  description: 'Discover the beauty of Azerbaijan with Baku Legend Travel. We offer exceptional travel experiences and tours in Baku and beyond.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
} 